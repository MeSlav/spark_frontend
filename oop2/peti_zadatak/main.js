class Vehicle {
    constructor(hasEngine, isElectric, canDrive, capacity, isRunning, mileage) {

    }

    drive() {
        
    }
}

class Car extends Vehicle {
    constructor() {
        super(hasEngine, isElectric, canDrive, capacity, isRunning, mileage);


    }
}

class Bike extends Vehicle {
    constructor() {
        super(hasEngine, isElectric, canDrive, capacity, isRunning, mileage);


    }
}